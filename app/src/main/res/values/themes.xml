<?xml version="1.0" encoding="utf-8"?>
<!--  res/values/themes/themes.xml  -->
<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Theme.IntegrationSDKTest" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorSecondary">@color/purple_200</item>
        <item name="colorOnSecondary">@color/white</item>

        <!-- TRANSPARENT status bar so toolbar bleeds behind clock/battery -->
        <item name="android:statusBarColor">@android:color/transparent</item>

        <!-- Light mode = dark status icons. We flip this in code based on theme. -->
        <item name="android:windowLightStatusBar" tools:targetApi="m">false</item>

        <!-- Bottom system nav bar matches our bottom nav -->
        <item name="android:navigationBarColor">@color/bottom_nav_background</item>
        <item name="android:windowLightNavigationBar" tools:targetApi="o_mr1">true</item>
    </style>
</resources>