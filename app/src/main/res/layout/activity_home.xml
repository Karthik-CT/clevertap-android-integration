<?xml version="1.0" encoding="utf-8"?>
<!--
    res/layout/activity_main.xml

    FIX: Use a simple vertical LinearLayout instead of CoordinatorLayout.
    This guarantees BottomNavigationView is ALWAYS visible — no dynamic
    margin overrides can accidentally hide it.

    The fragmentContainer gets weight=1 so it fills all space above the nav.
    Status bar inset is applied only to the toolbar inside the fragment.
-->
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <!-- Fragment host — fills all space above bottom nav -->
    <FrameLayout
        android:id="@+id/fragmentContainer"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"/>

    <!-- Bottom nav — ALWAYS at the bottom, height wraps its own content -->
    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:id="@+id/bottomNav"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/bottom_nav_background"
        app:menu="@menu/bottom_menu"
        app:labelVisibilityMode="labeled"
        app:itemIconTint="@drawable/bottom_nav_selector"
        app:itemTextColor="@drawable/bottom_nav_selector"
        app:elevation="8dp"/>

</LinearLayout>